{
  "name": "jakkzero-github-io",
  "version": "0.1.0",
  "description": "Jakkzero's personal website.",
  "scripts": {
    "start": "vite",
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "npm run build && git add dist -f && git commit -m 'deploying'  && git subtree push --prefix dist origin gh-pages"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.8.2",
    "vite": "^3.0.9",
    "vite-plugin-solid": "^2.3.0"
  },
  "dependencies": {
    "@hope-ui/solid": "^0.6.7",
    "@solidjs/router": "^0.4.3",
    "@stitches/core": "^1.2.8",
    "solid-js": "^1.5.1",
    "solid-transition-group": "^0.0.10"
  }
}
